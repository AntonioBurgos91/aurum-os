# AurumOS Continuous Integration (CI)

This directory hosts CI configuration scripts, custom workflow actions, and self-hosted runner automation logic.

## Pipelines
- **GitHub Actions workflows**: Custom workflows configured to build the desktop environment binaries and run the DL smoke tests on GPU-enabled infrastructure.
