# Fish seed config

Default Fish 4.0 config: `mise` hook, `uv` as the pip front-end, Starship
prompt init, DL quick-action aliases (`gpus`, `gpurestart`, `killpython`).
Deployed to `~/.config/fish/config.fish` via `/etc/skel`.
