# Ghostty seed config

Default [Ghostty](https://ghostty.org) terminal config: dark theme aligned
with Aurum.Aqua tokens, FiraCode mono font, generous scrollback. New users
on the installed system get a copy at `~/.config/ghostty/config`.
